<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DirectorioMAUI.Pages.EmpleadoPage">
    <Grid RowDefinitions="Auto, *">
        <Label Grid.Row="0" Text="{Binding Employees}" FontSize="24" FontAttributes="Bold" Margin="20,10" />

        <CollectionView Grid.Row="1" ItemsSource="{Binding Empleados}"
                SelectionMode="Single"
                SelectionChanged="OnEmpleadoSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,5" Padding="10" BackgroundColor="White" BorderColor="#EEEEEE" HasShadow="True" CornerRadius="10">
                        <Grid ColumnDefinitions="70, *, Auto" ColumnSpacing="15">

                            <Image Grid.Column="0"
                                   Source="{Binding ImagenCompleta}"
                                   WidthRequest="60"
                                   HeightRequest="60"
                                   Aspect="AspectFill"
                                   VerticalOptions="Center">
                                <Image.Clip>
                                    <EllipseGeometry Center="30,30" RadiusX="30" RadiusY="30" />
                                </Image.Clip>
                            </Image>

                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="2">
                                <HorizontalStackLayout Spacing="5">
                                    <Label Text="{Binding Nombre}" FontAttributes="Bold" FontSize="16" TextColor="Black" />
                                    <Label Text="{Binding Apellido}" FontAttributes="Bold" FontSize="16" TextColor="Black" />
                                </HorizontalStackLayout>

                                <Label Text="{Binding Id}" FontSize="13" TextColor="#666666" />
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <Label Text="No se encontraron empleados." HorizontalOptions="Center" VerticalOptions="Center" />
            </CollectionView.EmptyView>
        </CollectionView>
    </Grid>
</ContentPage>